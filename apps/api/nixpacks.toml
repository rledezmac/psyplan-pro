@"
[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/main.js"
"@ | Out-File -FilePath nixpacks.toml -Encoding UTF8